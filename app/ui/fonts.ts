import { Open_Sans, Inter } from 'next/font/google';

export const openSans = Open_Sans({
    subsets: ['latin'],
    weight: ['400', '600', '800'],
})

export const inter = Inter({
    subsets: ['latin'],
    weight: ['200']
})